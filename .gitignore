# Secrets — NEVER commit real env / keys
.env
*.key
secrets/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/
.venv/
venv/

# Tooling binaries (downloaded locally, not part of the repo)
mcp-publisher
mcp-publisher.exe
mcp-publisher.tar.gz
*.tar.gz

# OS
.DS_Store
Thumbs.db
